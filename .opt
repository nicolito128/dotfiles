#!/usr/bin/env sh

export DOTNET_ROOT="$HOME/opt/dotnet"

dirs=(
    "$DOTNET_ROOT"
    "$HOME/opt/odin"
)

# Add to PATH
for dir in "${dirs[@]}"; do
    if [ -d "$dir" ] && [[ ":$PATH:" != *":$dir:"* ]]; then
        export PATH="$dir:$PATH"
    fi
done

